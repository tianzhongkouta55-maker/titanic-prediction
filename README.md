# titanic-prediction
タイタニック予想を通じて，GitHubや機械学習などを知る
(AIを併用しています)


## Day 1: 基礎実装とプロセス理解（クリックで展開）

- **実施内容**: データ読み込み、`map`による数値化、決定木の実装
- **解決した課題**: パス指定による `FileNotFoundError` のデバッグ
- **学び**: `.fit`と`.predict`の役割の違い

<details>
<summary> 1. 学習内容と習得した技術 </summary>

#### 🛠 トラブルシューティング
- **FileNotFoundError への対応**: 
  プログラムがデータファイル（train.csv）を参照できないエラーに直面し、パスの指定、または外部URLからの直接読み込みによる解決方法を学習しました。

#### 🧪 実装した主な処理
- **データの前処理 (`map`関数)**: 
  機械学習モデルが計算を扱えるよう、文字データ（Sex）を数値に変換。リスト要素を一括処理する効率的な手法を習得しました。
- **f-string による出力**: 
  `f"..."` を活用し、型変換を意識せずに計算結果を文字列に埋め込む、可読性の高いコード記述を実践しました。

#### 🤖 機械学習アルゴリズムとプロセス
- **DecisionTreeClassifier (決定木)**: 
  「Yes/No」の条件分岐を繰り返してデータを分類するアルゴリズムを理解しました。
- **学習と予測の分離**: 
  - **`.fit` (学習)**: 過去のデータから解き方を導き出す「インプット」の工程。
  - **`.predict` (予測)**: 学習したルールを新しいデータに適用する「アウトプット」の工程。
  この2つの明確な役割の違いを整理しました。

### 2. 今後の課題
- 特徴量（ヒントとなるデータ）を増やし、正解率の向上を目指す。
- モデルの過学習（練習問題にだけ強くなる現象）を防ぐための設定を学ぶ。

### 3. 本日の成果
- **検証スコア（正解率）**: 78.77%%
  （※性別・客室ランクのみを特徴量として使用）
</details>

## Day 2: 過学習の特定と特徴量エンジニアリングによる本質的ルールの抽出（クリックで展開）

### 1. 実施内容：モデルの最適化と特徴量生成
*   **決定木モデルの構築と制御**: `max_depth` パラメータを用いた学習の制限。
*   **特徴量エンジニアリング**: 既存の変数（SibSp, Parch）を統合し、新変数 **`FamilySize`** を作成。
*   **データ補完の実装**: `.fillna()` を用い、統計的根拠に基づいた欠損値（Age/Fare）の補完。

### 2. 解決した課題：過学習の抑制とノイズの排除
*   **過学習の特定**: 訓練データ（98.17%）とテストデータ（79.33%）の大きな乖離を、モデルの「丸暗記」によるものと特定し、階層制限により解消。
*   **重要度スコアの健全化**: 運賃や年齢などの「細かすぎる数値データ」に判断が偏っていた状態を改善し、汎化性能を向上。
*   **予測精度の安定**: テストデータの正解率を **82.12%** まで改善・安定化。

### 3. 学び：データの本質を捉える重要性
*   **多角的な評価**: 正解率だけでなく、重要度スコアから「AIが納得感のある根拠で判断しているか」を検証する重要性を習得。
*   **ドメイン知識の活用**: 単体では寄与が低いデータも、背景知識（家族という集団性）に基づき統合することで、強力な指標へ昇華できることを実証。

---
<details>
<summary>1. 特徴量追加による精度の変動と考察 </summary>
モデル構築の過程で、以下の挙動を確認し分析を行いました。

*   **Age（年齢）**: 追加により正答率が上昇。
*   **Fare（運賃）/ SibSp（兄弟・配偶者）**: 追加により正答率が減少。訓練データに特化しすぎる**過学習（Overfitting）**の傾向を確認。
*   **重要度分析**: `Sex` > `Fare` > `Age` の順でスコアを算出。
    *   **考察**: `Fare` や `Age` が高いのは、数値が細かすぎるためにAIが「個人の特定」にデータを利用している（ノイズを学習している）状態だと判断。

### 2. 施策と改善結果

#### A. モデルの剪定（Pruning）
AIの学習範囲を制限し、細かい数値の差異ではなく「データの大きな塊」で判断するよう制御。
*   **結果**: 過学習が抑制され、未知のデータに対するスコアが向上。

#### B. 特徴量の統合
重要度スコアが低かった `SibSp` と `Parch`（親子）を統合し、`FamilySize` を生成。
*   **結果**: 正答率の直接的な劇的変化はなかったが、**重要度スコアが大幅に上昇**。データの「見せ方」を変えることで、不要に見えたデータが予測に不可欠な要素へ変わることを確認。

### 3. 使用技術・メソッド
*   **`.fillna()`**: 欠損値補完。運賃には平均値（mean）、年齢には中央値（median）を採用。
*   **特徴量エンジニアリング**: 既存データの組み合わせによる新しい意味の抽出。

---

## ■ 今後の展望
*   82%の壁を突破するため、複数の決定木を組み合わせる「アンサンブル学習（ランダムフォレスト等）」の導入。
*   さらに精度を高めるためのカテゴリ変数の最適化。

</details> 


## Day 3: アンサンブル学習の導入と多重共線性へのアプローチ（クリックで展開）

### 1. 実施内容：Random Forestへの移行とモデルの安定化
*   **アンサンブル学習の実装**: 複数の決定木を組み合わせる **`RandomForestClassifier`** を導入。
*   **モデルの調整**: 過学習を抑制するため、層の深さ（`max_depth`）を 調整．
*   **ハイパーパラメータの自動探索**: 最適な学習パラメータの目安を自動で探査（Grid Search等の検討）するプロセスを導入。

### 2. 解決した課題：相関の強い特徴量の整理と欠損値補完の高度化
*   **多重共線性（Multicollinearity）の制御**: 「敬称（Title）」「Sex」「Age」の強い相関による悪影響を検証。
*   **敬称（Title）の最適化**: 敬称を単に追加するのではなく、「大人（男女）」と「子供」に集約して区分することで、精度を **0.76%** 向上。
*   **Age補完の精密化**: 全体の中央値ではなく、敬称グループごとの中央値で補完するロジックに変更し、`Age` の重要度スコア（Feature Importance）を改善。

### 3. 学び：精度と汎化性能のトレードオフ
*   **データの解像度と乖離**: `Age` の補完を精密にした結果、重要度は上がったが、訓練スコアとテストスコアの差が **4.68%** に拡大。モデルが訓練データに対して「作り込みすぎる」ことで生じる過学習のリスクを再認識。
*   **質的データの変換**: 敬称のような質的データを、どのようにグルーピングすればAIにとって「意味のある境界線」になるかを試行錯誤する重要性を習得。

---
<details>
<summary>1. モデルの評価スコアと検証プロセス </summary>*

####  特徴量エンジニアリングの試行錯誤
モデルの挙動を観察し、以下の施策を検証しました。

1.  **Sex（性別）の除外検証**: 敬称と役割が被る `Sex` をあえて削除。
    *   **結果**: 精度に差は生じなかった。余分な計算を削減に成功．
2.  **敬称（Title）のセグメント化**:
    *   **結果**: 大人/子供の区分を最適化したことで精度が向上。データの「粒度」を調整する有効性を確認。
3.  **Ageの個別補完**: 敬称グループごとに補完値を変える手法。
    *   **結果**: `Age` の重要度が上昇。しかし、訓練データへの特化が進み、テストデータとの乖離（4.68%）が発生。

### 2. 使用技術・メソッド
*   **`RandomForestClassifier`**: 多数決アルゴリズムによる予測の安定化。
*   **Hyperparameter Tuning**: パラメータの自動探索によるモデルの底上げ。
*   **Groupby-Imputation**: 属性に基づいた高度な欠損値補完。

---

## ■ 今後の展望
*   4.68%まで開いた訓練・テストデータの乖離を抑えるための特徴量の調整。
*   RandamForest以外のアルゴリズムの使用．
</details>

---


*Created by [tianzhongkouta55-maker] - 2026/05/03*
*## Day 5: 開発環境の安定化と特徴量の最終評価

**【要約】**
性別データの追加による予測因子の強化と、パス指定や変数名の不整合といった実装上のエラーを完全に解消しました。精度は **0.82** で安定しており、前処理から提出用CSVの書き出しまでがエラーなく一気通貫で動く、堅牢なパイプラインを構築しました。

---

<details>
<summary><b>詳細な実施内容と技術的プロセス（クリックで展開）</b></summary>

### 1. 実施内容：モデルの最終調整とエラー要因の特定
*   **特徴量の再選定**: 最強の予測因子である **`Sex`（性別）** を前処理パイプラインに統合。
*   **デバッグ作業の徹底**: 変数定義ミス（`NameError`）やデータ型不一致（`TypeError`）など、VS Codeでの実行を妨げる要因を全て排除。
*   **予測結果の出力実装**: Kaggle提出用の形式（PassengerId, Survived）に準拠したCSV書き出しロジックの構築。

### 2. 解決した課題：開発フローにおける「詰まり」の解消
*   **変数のスコープ管理**: データの読み込み名と関数への投入名が一致しない問題を解決。
*   **多値戻り値の処理**: 関数から返されるタプルデータを、アンパック代入（`X, y = ...`）を用いて適切に処理する手法を確立。
*   **相対パスから絶対パスへの最適化**: ローカル環境のディレクトリ構造に依存しない安定したデータロードを実現。

### 3. 学び：特徴量の相関と予測の「壁」
*   **情報の重複（多重共線性への気づき）**: `Sex` を追加しても精度が変化しなかったことで、`Title`（敬称）に既に性別情報が含まれていたことをデータ的に理解。
*   **0.82の壁とノイズの存在**: アルゴリズムが捉えきれない「不運な死」などのノイズを認識し、さらなる改善にはより高度な複合特徴量が必要であることを習得。

---

#### ■ 実装のポイントと精度の推移
1.  **性別情報の数値化**:
    *   **結果**: male/femaleを0/1に変換。モデルの判断根拠をより明確化。
2.  **提出データの整合性確保**:
    *   **結果**: `index=False` 指定により、Kaggleでのフォーマットエラーを未然に防止。
3.  **検証用データの安定性**:
    *   **結果**: 常に **0.82** 前後の **Accuracy** を維持。過学習を抑えた汎用的な学習を確認。

#### ■ 使用技術・メソッド
*   **`Pandas Mapping`**: カテゴリデータの数値変換。
*   **`Tuple Unpacking`**: 関数の戻り値を効率的に分配するPythonの記法。
*   **`Submission Formatting`**: 提出用DataFrameの構築と閾値判定処理。

---

#### ■ 今後の展望
*   `0.82` を突破するための新指標「Age × Pclass（客室クラス別年齢）」などの複合特徴量の導入。
*   Kaggleへの初提出を行い、ローカルの検証スコアとグローバル順位の相関を確認。

</details>                                  - 2026/05/04*
                                     - 2026/05/06